import { LimitCharatchersPipe } from './limit-charatchers.pipe';

describe('LimitCharatchersPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCharatchersPipe();
    expect(pipe).toBeTruthy();
  });
});
